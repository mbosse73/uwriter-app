# U-Writer ist bewusst abhängigkeitsfrei: im Repo darf keine Node-Toolchain
# entstehen. Verifikation (jsdom) läuft ausschließlich in einem Scratch-
# Verzeichnis außerhalb des Repos — siehe CLAUDE.md, Abschnitt "Commands".
node_modules/
package.json
package-lock.json
npm-debug.log*

# Snapshots der Anwendung gehören in git-Tags, nicht als Kopie ins Repo
# (ANALYSIS.md, M-8). Alter Stand: Tag backups-archiv-20260813.
ulysses.backup-*.html

# Betriebssystem- und Editor-Beiwerk
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Lokale Claude-Code-Einstellungen (die geteilte settings.json ist versioniert)
.claude/settings.local.json
